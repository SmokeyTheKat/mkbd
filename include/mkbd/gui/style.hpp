#ifndef __MKBD_STYLE_HPP__
#define __MKBD_STYLE_HPP__

namespace WindowStyle {
	const Color fgColor = Color(186, 238, 255);
	const Color bgColor = Color(176, 205, 219);
	const Color accColor = Color(8, 119, 188);
	const Color borderColor = Color(150, 150, 150);

	const int headerHeight = 60;
	const int pianoControlHeight = 60;

	const int menuWidth = 300;
};

#endif